let imageFolder = '../images/';
let textFolder = '../preprocessed';

// const cv = require('node-opencv')

// const preprocess = (name) => {
    // cv.readImage(imageFolder + name, (err, img) => {
        // if (err){
            // throw err;
        // }
//
        // const width = img.width();
        // const height = img.height();
//
        // if (width < 1 || height < 1)
            // throw new Error("Image hs no size")
//
        // console.log(width, height)
//
        // img.save(textFolder + name)
    // })
// }
//
// module.exports = {
    // preprocess
// }
